# mocks

Prototype mock data and repositories.

## Purpose
Provides browser-only mock data and fake API behaviour for local development and demos.

## Contains
- Mock repositories (auth, family, admin, etc.)
- Sample/mock data

## Notes
- Not used in production
- Replace with real `shared/api` services when backend endpoints are available
- Keep mocks thin; avoid putting real business logic here