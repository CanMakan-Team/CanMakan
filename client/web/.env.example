VITE_API_BASE_URL=http://localhost:8080
# Live auth + UC8 family create/me by default. Set true only for unfinished
# family/admin mock surfaces (members, history, admin pages).
VITE_USE_MOCK_API=false
