package com.canmakan.backend.notification.model;

/**
 * Inbox card kinds. Family invite types are one of several possible sources.
 */
public enum NotificationType {
    FAMILY_INVITE_UPDATE,
    FAMILY_INVITE_REQUEST
}
