# Machine Learning

This reserved component will contain future data preparation, models,
experiments, evaluation, and inference logic for ingredient interpretation.

Technology selection is intentionally pending. The team has not yet approved a
Python version, package manager, ML framework, or project layout beyond the
lightweight `src/` and `tests/` placeholders. Do not initialise a framework or
add runtime dependencies until those decisions are confirmed.
